## 1. 前端頁面實作

- [x] 1.1 將 `forgot-password.vue` 從 stub 改為完整頁面，以 `register.vue` 為模板，調整 API path（`/forgot-password`）、權限 menuId（`MENU.VerificationForgotPassword`）、批次重發 endpoint（`/forgot-password/resend`）

## 2. 驗證

- [x] 2.1 啟動 Admin Nuxt App，在瀏覽器中測試頁面功能（列表載入、搜尋篩選、排序、分頁、重發按鈕顯示）
